# The project declares engines.node = "20.x", but this environment's active
# node binary is v22.x. The scripts run via tsx/TypeScript and are compatible
# with node 22, so skip the engine compatibility check for all yarn commands.
--ignore-engines true
